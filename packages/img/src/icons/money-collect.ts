/**
 * @format
 * @file MoneyCollect 收款
 * @author 由 fe6 自动生成
 */

import { ISvgIconProps, IconWrapper } from '../runtime';

// 获取 SVG 的 HTML 字符串
export const getIconMoneyCollectSvgHtml = (props: ISvgIconProps) =>
  `<?xml version="1.0" encoding="UTF-8"?>
<svg width="${props.size}" height="${props.size}" viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
  <g/>
</svg>`;

// 默认导出组件
export default IconWrapper('money-collect', false, getIconMoneyCollectSvgHtml);
