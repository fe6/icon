/**
 * @format
 * @file BytedElectronicPen byted-electronic-pen
 * @author 由 fe6 自动生成
 */

import { IIconProps, IconWrapper } from '../runtime';

// 获取 SVG 的 HTML 字符串
export const getIconBytedElectronicPenSvgHtml = (props: IIconProps) =>
  `<?xml version="1.0" encoding="UTF-8"?>
<svg width="${props.size}" height="${
    props.size
  }" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
  <g clip-path="${`url(#${props.id}img0)`}">
    <rect x="35.1924" y="5.32239" width="12" height="38" rx="6" transform="rotate(45 35.1924 5.32239)" fill="${
      props.colors?.[1]
    }" stroke="${props.colors?.[0]}" stroke-width="${props.strokeWidth}"/>
    <path d="M18 23L26 31" stroke="${props.colors?.[0]}" stroke-width="${
    props.strokeWidth
  }" stroke-linecap="${props.strokeLinecap}" stroke-linejoin="${
    props.strokeLinejoin
  }"/>
    <path d="M6 43L12 37" stroke="${props.colors?.[0]}" stroke-width="${
    props.strokeWidth
  }" stroke-linecap="${props.strokeLinecap}" stroke-linejoin="${
    props.strokeLinejoin
  }"/>
    <rect x="33.2679" y="12.3397" width="4" height="4" rx="2" transform="rotate(30 33.2679 12.3397)" fill="${
      props.colors?.[2]
    }"/>
  </g>;
  <defs/>
</svg>`;

// 默认导出组件
export default IconWrapper(
  'byted-electronic-pen',
  false,
  getIconBytedElectronicPenSvgHtml,
);
